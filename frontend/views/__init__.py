import string
import random
import gnupg

gpg = gnupg.GPG("gpg")




# ascii_armored_public_keys = gpg.export_keys(gpg_key_pair)
# ascii_armored_private_keys = gpg.export_keys(gpg_key_pair, True)
# with open('mykeyfile.asc', 'wb') as f:
#     f.write(ascii_armored_public_keys)
#     f.write(ascii_armored_private_keys)

